import React from 'react';

const ColeccionesPage: React.FC = () => {
  return (
    <div>
      <h1>Colecciones</h1>
      <p>Bienvenido a la página de colecciones.</p>
    </div>
  );
};

export default ColeccionesPage;